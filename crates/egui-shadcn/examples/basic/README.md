# Basic Example

This example shows a single screen that renders all core `egui-shadcn` form components with the default theme and interactive state toggles.

## Run

```
cargo run --example basic
```

